import React, { useState } from "react";
import { FormControl, Input, InputLabel } from "@mui/material";
import { IFormSupplies } from "@/interfaces/formSuppliesInterface";

export default function FormSupplies(): React.ReactElement{
    return(
        <div>ds</div>
    );
}